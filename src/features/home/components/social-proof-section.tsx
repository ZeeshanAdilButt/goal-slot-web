// Intentionally rendered as null until we have honest social proof
// (real customer logos, real quotes, real metrics). Do NOT add fabricated
// trust-bar content here — the team has explicitly asked for accuracy.

export function SocialProofSection() {
  return null
}
