# API URL
NEXT_PUBLIC_API_URL='BACKEND_URL'

# App Port
PORT="THIS APP'S PORT"

# Supabase (for client-side auth if needed)
NEXT_PUBLIC_SUPABASE_URL="SUPABASE_URL"
NEXT_PUBLIC_SUPABASE_ANON_KEY="SUPABASE_KEY"

# Messaging (jiffy-messaging). Leave both unset to keep the Messages surface
# hidden: no nav entry, no buttons on Sharing, and /dashboard/messages renders
# a "not enabled" notice instead of failing.
# Base HTTP URL of the messaging service, no trailing slash.
NEXT_PUBLIC_MESSAGING_URL=""
# Optional. Derived from the URL above (http -> ws, https -> wss) when unset;
# only set it when the WebSocket lives on a different host or scheme.
NEXT_PUBLIC_MESSAGING_WS_URL=""

# Required: PostHog Project API Key (starts with phc_)
NEXT_PUBLIC_POSTHOG_KEY=phc_your_project_api_key_here

# Required: PostHog Host URL (e.g., https://us.i.posthog.com or your self-hosted instance)
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Optional: For sourcemaps upload (helps with error tracking)
# Personal API Key (starts with phx_)
POSTHOG_PERSONAL_API_KEY=phx_your_personal_api_key_here

# Optional: PostHog Environment ID
POSTHOG_ENV_ID=your_environment_id_here